package challenge.fiap.repositories;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public abstract class _BaseRepo {
    protected Logger LOGGER = LogManager.getLogger(getClass());
}
